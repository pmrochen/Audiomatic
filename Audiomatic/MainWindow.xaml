<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Audiomatic.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Audiomatic">

    <Grid x:Name="RootGrid" KeyDown="RootGrid_KeyDown"
          RightTapped="RootGrid_RightTapped"
          AllowDrop="True"
          DragOver="RootGrid_DragOver"
          Drop="RootGrid_Drop">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Row 0: Custom title bar (drag area) -->
        <Grid x:Name="CustomTitleBar" Grid.Row="0" Height="32" Background="Transparent"
              Padding="12,0"
              PointerPressed="DragArea_PointerPressed"
              PointerMoved="DragArea_PointerMoved"
              PointerReleased="DragArea_PointerReleased">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Collapse/Expand toggle -->
            <Button x:Name="CollapseButton" Grid.Column="1"
                    Background="Transparent" BorderThickness="0"
                    Padding="6,4" Click="CollapseToggle_Click"
                    ToolTipService.ToolTip="Compact (Ctrl+L)">
                <FontIcon x:Name="CollapseIcon" Glyph="&#xE73F;" FontSize="11"/>
            </Button>

            <Button x:Name="PinButton" Grid.Column="2"
                    Background="Transparent" BorderThickness="0"
                    Padding="6,4" Click="Pin_Click"
                    ToolTipService.ToolTip="Pin on top">
                <FontIcon x:Name="PinIcon" Glyph="&#xE840;" FontSize="11"/>
            </Button>

            <Button Grid.Column="3" Background="Transparent" BorderThickness="0"
                    Padding="6,4" Click="Close_Click">
                <FontIcon Glyph="&#xE711;" FontSize="11"/>
            </Button>
        </Grid>

        <!-- Row 1: Now Playing card (also drag area) -->
        <Grid x:Name="NowPlayingCard" Grid.Row="1" Padding="14,4,14,8"
              Background="Transparent"
              PointerPressed="DragArea_PointerPressed"
              PointerMoved="DragArea_PointerMoved"
              PointerReleased="DragArea_PointerReleased">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Album art -->
            <Grid Grid.Column="0" Width="56" Height="56" CornerRadius="6"
                  Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                <Image x:Name="AlbumArtImage" Stretch="UniformToFill"
                       Width="56" Height="56"/>
                <FontIcon x:Name="AlbumArtPlaceholder" Glyph="&#xE8D6;" FontSize="24"
                          HorizontalAlignment="Center" VerticalAlignment="Center"
                          Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
            </Grid>

            <!-- Track info -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="1" Margin="12,0,0,0">
                <TextBlock x:Name="TrackTitle" Text="No track"
                           FontWeight="SemiBold" FontSize="14"
                           TextTrimming="CharacterEllipsis" MaxLines="1"/>
                <TextBlock x:Name="TrackArtist" Text=""
                           Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           TextTrimming="CharacterEllipsis" MaxLines="1"/>
                <TextBlock x:Name="TrackAlbum" Text=""
                           Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                           TextTrimming="CharacterEllipsis" MaxLines="1"
                           FontSize="11"/>
            </StackPanel>
        </Grid>

        <!-- Row 1b: Mini-player layout (visible only in mini mode) -->
        <Grid x:Name="MiniPlayerBar" Grid.Row="1" Padding="10,6,10,6"
              Background="Transparent" Visibility="Collapsed"
              PointerPressed="DragArea_PointerPressed"
              PointerMoved="DragArea_PointerMoved"
              PointerReleased="DragArea_PointerReleased">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Mini album art -->
            <Grid Grid.Column="0" Width="40" Height="40" CornerRadius="4"
                  Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                <Image x:Name="MiniAlbumArt" Stretch="UniformToFill"
                       Width="40" Height="40"/>
                <FontIcon x:Name="MiniAlbumArtPlaceholder" Glyph="&#xE8D6;" FontSize="18"
                          HorizontalAlignment="Center" VerticalAlignment="Center"
                          Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
            </Grid>

            <!-- Title - Artist on one line -->
            <TextBlock x:Name="MiniTrackText" Grid.Column="1"
                       Text="No track" FontSize="12"
                       VerticalAlignment="Center" Margin="10,0,0,0"
                       TextTrimming="CharacterEllipsis" MaxLines="1"/>

            <!-- Play/Pause button -->
            <Button x:Name="MiniPlayPauseButton" Grid.Column="2"
                    Width="32" Height="32" CornerRadius="16"
                    Padding="0" Margin="6,0"
                    Background="{ThemeResource AccentFillColorDefaultBrush}"
                    Foreground="White"
                    HorizontalContentAlignment="Center"
                    VerticalContentAlignment="Center"
                    Click="PlayPause_Click">
                <FontIcon x:Name="MiniPlayPauseIcon" Glyph="&#xE768;" FontSize="14"
                          Foreground="White"/>
            </Button>

            <!-- Close button -->
            <Button Grid.Column="3" Background="Transparent" BorderThickness="0"
                    Padding="4,4" Click="Close_Click">
                <FontIcon Glyph="&#xE711;" FontSize="10"/>
            </Button>
        </Grid>

        <!-- Row 2: Timeline (progress bar + times) -->
        <Grid Grid.Row="2" Padding="14,0,14,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Slider x:Name="TimelineSlider" Grid.Row="0"
                    Minimum="0" Maximum="100" Value="0"
                    Height="24"
                    ValueChanged="TimelineSlider_ValueChanged"/>

            <Grid Grid.Row="1" Margin="0,-4,0,0">
                <TextBlock x:Name="PositionText" Text="0:00"
                           Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                           FontSize="11"
                           HorizontalAlignment="Left"/>
                <TextBlock x:Name="DurationText" Text="0:00"
                           Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                           FontSize="11"
                           HorizontalAlignment="Right"/>
            </Grid>
        </Grid>

        <!-- Row 3: Playback controls -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="4"
                    HorizontalAlignment="Center" Padding="14,4,14,4">
            <Button x:Name="ShuffleButton" Background="Transparent" BorderThickness="0"
                    Padding="8,6" Click="Shuffle_Click"
                    ToolTipService.ToolTip="Shuffle" VerticalAlignment="Center">
                <FontIcon x:Name="ShuffleIcon" Glyph="&#xE8B1;" FontSize="14"/>
            </Button>
            <Button x:Name="PrevButton" Background="Transparent" BorderThickness="0"
                    Padding="8,6" Click="Prev_Click"
                    ToolTipService.ToolTip="Previous" VerticalAlignment="Center">
                <FontIcon Glyph="&#xE892;" FontSize="18"/>
            </Button>

            <!-- Play/Pause: large round filled button -->
            <Button x:Name="PlayPauseButton"
                    Width="48" Height="48" CornerRadius="24"
                    Padding="0" Margin="4,0"
                    Background="{ThemeResource AccentFillColorDefaultBrush}"
                    Foreground="White"
                    HorizontalContentAlignment="Center"
                    VerticalContentAlignment="Center"
                    Click="PlayPause_Click">
                <FontIcon x:Name="PlayPauseIcon" Glyph="&#xE768;" FontSize="22"
                          Foreground="White"/>
            </Button>

            <Button x:Name="NextButton" Background="Transparent" BorderThickness="0"
                    Padding="8,6" Click="Next_Click"
                    ToolTipService.ToolTip="Next" VerticalAlignment="Center">
                <FontIcon Glyph="&#xE893;" FontSize="18"/>
            </Button>
            <Button x:Name="RepeatButton" Background="Transparent" BorderThickness="0"
                    Padding="8,6" Click="Repeat_Click"
                    ToolTipService.ToolTip="Repeat" VerticalAlignment="Center">
                <FontIcon x:Name="RepeatIcon" Glyph="&#xE8EE;" FontSize="14"/>
            </Button>
        </StackPanel>

        <!-- Row 3b: Volume bar (separate row) -->
        <Grid x:Name="VolumeRow" Grid.Row="4" Padding="14,0,14,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Background="Transparent" BorderThickness="0"
                    Padding="4,4" Click="Mute_Click" VerticalAlignment="Center">
                <FontIcon x:Name="VolumeIcon" Glyph="&#xE995;" FontSize="13"/>
            </Button>
            <Slider x:Name="VolumeSlider" Grid.Column="1"
                    Minimum="0" Maximum="100" Value="100"
                    Height="24"
                    ValueChanged="Volume_Changed"
                    VerticalAlignment="Center"/>
        </Grid>

        <!-- Row 5: View navigation -->
        <Grid x:Name="NavRow" Grid.Row="5" Padding="14,0,14,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Tab buttons (Library / Playlists) -->
            <StackPanel x:Name="NavTabs" Grid.Column="0" Orientation="Horizontal" Spacing="2">
                <Button x:Name="NavLibraryBtn" Background="Transparent" BorderThickness="0"
                        Padding="8,4" Click="NavLibrary_Click">
                    <TextBlock x:Name="NavLibraryText" Text="Library" FontSize="12" FontWeight="SemiBold"/>
                </Button>
                <Button x:Name="NavPlaylistsBtn" Background="Transparent" BorderThickness="0"
                        Padding="8,4" Click="NavPlaylists_Click">
                    <TextBlock x:Name="NavPlaylistsText" Text="Playlists" FontSize="12"/>
                </Button>
                <Button x:Name="NavQueueBtn" Background="Transparent" BorderThickness="0"
                        Padding="8,4" Click="NavQueue_Click">
                    <TextBlock x:Name="NavQueueText" Text="Queue" FontSize="12"/>
                </Button>
                <Button x:Name="NavRadioBtn" Background="Transparent" BorderThickness="0"
                        Padding="8,4" Click="NavRadio_Click">
                    <TextBlock x:Name="NavRadioText" Text="Radio" FontSize="12"/>
                </Button>
                <Button x:Name="NavPodcastBtn" Background="Transparent" BorderThickness="0"
                        Padding="8,4" Click="NavPodcast_Click">
                    <TextBlock x:Name="NavPodcastText" Text="Podcasts" FontSize="12"/>
                </Button>
                <Button x:Name="NavMoreBtn" Background="Transparent" BorderThickness="0"
                        Padding="8,4" Click="NavMore_Click">
                    <TextBlock x:Name="NavMoreText" Text="..." FontSize="12"/>
                </Button>
            </StackPanel>

            <!-- Playlist detail header (back + name) -->
            <StackPanel x:Name="PlaylistHeader" Grid.Column="0" Orientation="Horizontal"
                        Spacing="4" Visibility="Collapsed">
                <Button Background="Transparent" BorderThickness="0" Padding="4,4"
                        Click="NavBack_Click" ToolTipService.ToolTip="Back to playlists">
                    <FontIcon Glyph="&#xE72B;" FontSize="12"/>
                </Button>
                <TextBlock x:Name="PlaylistNameText" VerticalAlignment="Center"
                           FontSize="12" FontWeight="SemiBold"
                           TextTrimming="CharacterEllipsis" MaxLines="1"/>
            </StackPanel>

            <!-- Album detail header (back + album name) -->
            <StackPanel x:Name="AlbumHeader" Grid.Column="0" Orientation="Horizontal"
                        Spacing="4" Visibility="Collapsed">
                <Button Background="Transparent" BorderThickness="0" Padding="4,4"
                        Click="AlbumBack_Click" ToolTipService.ToolTip="Back to albums">
                    <FontIcon Glyph="&#xE72B;" FontSize="12"/>
                </Button>
                <TextBlock x:Name="AlbumNameText" VerticalAlignment="Center"
                           FontSize="12" FontWeight="SemiBold"
                           TextTrimming="CharacterEllipsis" MaxLines="1"/>
            </StackPanel>

            <!-- New playlist button -->
            <Button x:Name="NewPlaylistBtn" Grid.Column="1" Background="Transparent"
                    BorderThickness="0" Padding="6,4" Click="NewPlaylist_Click"
                    Visibility="Collapsed" ToolTipService.ToolTip="New Playlist">
                <FontIcon Glyph="&#xE710;" FontSize="12"/>
            </Button>

            <!-- Clear queue button -->
            <Button x:Name="ClearQueueBtn" Grid.Column="1" Background="Transparent"
                    BorderThickness="0" Padding="6,4" Click="ClearQueue_Click"
                    Visibility="Collapsed" ToolTipService.ToolTip="Clear Queue">
                <FontIcon Glyph="&#xE74D;" FontSize="12"/>
            </Button>
        </Grid>

        <!-- Row 6: Search + Sort -->
        <Grid x:Name="SearchSortRow" Grid.Row="6" Padding="14,0,14,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchBox" Grid.Column="0"
                     PlaceholderText="Search"
                     FontSize="13"
                     Padding="8,6"
                     CornerRadius="6"
                     TextChanged="SearchBox_TextChanged">
                <TextBox.BorderBrush>
                    <SolidColorBrush Color="{ThemeResource ControlStrokeColorDefault}"/>
                </TextBox.BorderBrush>
            </TextBox>

            <DropDownButton x:Name="SortButton" Grid.Column="1" Margin="6,0,0,0"
                            Padding="8,6">
                <StackPanel Orientation="Horizontal" Spacing="4">
                    <FontIcon Glyph="&#xE8CB;" FontSize="12"/>
                </StackPanel>
                <DropDownButton.Flyout>
                    <MenuFlyout>
                        <ToggleMenuFlyoutItem x:Name="SortTitle" Text="Title" Click="Sort_Click" Tag="title" IsChecked="True"/>
                        <ToggleMenuFlyoutItem x:Name="SortArtist" Text="Artist" Click="Sort_Click" Tag="artist"/>
                        <ToggleMenuFlyoutItem x:Name="SortAlbum" Text="Album" Click="Sort_Click" Tag="album"/>
                        <ToggleMenuFlyoutItem x:Name="SortDuration" Text="Duration" Click="Sort_Click" Tag="duration"/>
                        <MenuFlyoutSeparator/>
                        <ToggleMenuFlyoutItem x:Name="SortAscending" Text="Ascending" IsChecked="True" Click="SortDirection_Click"/>
                    </MenuFlyout>
                </DropDownButton.Flyout>
            </DropDownButton>
        </Grid>

        <!-- Row 7: Track list -->
        <ListView x:Name="TrackListView" Grid.Row="7"
                  Padding="6,0"
                  SelectionMode="Single"
                  IsItemClickEnabled="True"
                  ItemClick="TrackList_ItemClick"
                  DragItemsCompleted="TrackListView_DragItemsCompleted">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="8,4"/>
                    <Setter Property="MinHeight" Value="48"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <!-- Row 7: Waveform visualizer (shown when Visualizer view is active) -->
        <Grid x:Name="WaveformContainer" Grid.Row="7"
              Visibility="Collapsed"
              Background="Transparent"
              SizeChanged="WaveformContainer_SizeChanged"
              PointerPressed="WaveformCanvas_PointerPressed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel x:Name="VisualizerSelector" Grid.Row="0"/>
            <Grid x:Name="VisualizerCanvasHost" Grid.Row="1"/>
            <Canvas x:Name="WaveformCanvas" Grid.Row="1"/>
        </Grid>

        <!-- Row 7: Radio (shown when Radio view is active) -->
        <ScrollViewer x:Name="RadioContainer" Grid.Row="7" Visibility="Collapsed"
                      Padding="14,0">
            <StackPanel Spacing="12">
                <TextBlock Text="Radio Stream" FontSize="14" FontWeight="SemiBold"
                           Margin="0,8,0,0"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="RadioUrlBox" Grid.Column="0"
                             PlaceholderText="Enter stream URL (e.g. http://...)"
                             FontSize="13" Padding="8,6" CornerRadius="6"
                             KeyDown="RadioUrlBox_KeyDown"/>
                    <Button x:Name="RadioPlayBtn" Grid.Column="1" Margin="6,0,0,0"
                            Padding="10,6" CornerRadius="6" Click="RadioPlay_Click"
                            ToolTipService.ToolTip="Play stream">
                        <FontIcon Glyph="&#xE768;" FontSize="14"/>
                    </Button>
                </Grid>
                <TextBlock x:Name="RadioStatusText" Text=""
                           Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           TextWrapping="Wrap"/>
                <ListView x:Name="RadioHistoryList"
                          SelectionMode="None"
                          IsItemClickEnabled="True"
                          ItemClick="RadioHistory_ItemClick">
                    <ListView.Header>
                        <TextBlock Text="Recent stations" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                   Margin="0,4,0,4"/>
                    </ListView.Header>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="4,2"/>
                            <Setter Property="MinHeight" Value="32"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </StackPanel>
        </ScrollViewer>

        <!-- Row 7: Podcast (shown when Podcast view is active) -->
        <Grid x:Name="PodcastContainer" Grid.Row="7" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Search bar + back button -->
            <Grid Grid.Row="0" Padding="14,4,14,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button x:Name="PodcastBackBtn" Grid.Column="0" Background="Transparent"
                        BorderThickness="0" Padding="4,4" Click="PodcastBack_Click"
                        Visibility="Collapsed" ToolTipService.ToolTip="Back">
                    <FontIcon Glyph="&#xE72B;" FontSize="12"/>
                </Button>
                <TextBox x:Name="PodcastSearchBox" Grid.Column="1"
                         PlaceholderText="Search podcasts..."
                         FontSize="13" Padding="8,6" CornerRadius="6"
                         KeyDown="PodcastSearchBox_KeyDown"/>
                <Button x:Name="PodcastSearchBtn" Grid.Column="2" Margin="6,0,0,0"
                        Padding="10,6" CornerRadius="6" Click="PodcastSearch_Click"
                        ToolTipService.ToolTip="Search">
                    <FontIcon Glyph="&#xE721;" FontSize="14"/>
                </Button>
            </Grid>

            <!-- Content list -->
            <ListView x:Name="PodcastListView" Grid.Row="1"
                      Padding="6,0"
                      SelectionMode="None"
                      IsItemClickEnabled="True"
                      ItemClick="PodcastList_ItemClick">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="8,4"/>
                        <Setter Property="MinHeight" Value="48"/>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </Grid>

        <!-- Row 7: Albums grid (shown when Albums view is active) -->
        <GridView x:Name="AlbumsGridView" Grid.Row="7"
                  Visibility="Collapsed"
                  Padding="6,0"
                  SelectionMode="None"
                  IsItemClickEnabled="True"
                  ItemClick="AlbumGrid_ItemClick">
            <GridView.ItemContainerStyle>
                <Style TargetType="GridViewItem">
                    <Setter Property="Margin" Value="4"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </GridView.ItemContainerStyle>
        </GridView>

        <!-- Row 7: Equalizer (shown when Equalizer view is active) -->
        <ScrollViewer x:Name="EqualizerContainer" Grid.Row="7" Visibility="Collapsed"
                      Padding="14,0">
            <StackPanel x:Name="EqualizerPanel" Spacing="10"/>
        </ScrollViewer>

        <!-- Row 7: Media control (shown when Media view is active) -->
        <ScrollViewer x:Name="MediaContainer" Grid.Row="7" Visibility="Collapsed"
                      Padding="6,0">
            <StackPanel x:Name="MediaSessionList" Spacing="4"/>
        </ScrollViewer>

        <!-- Row 8: Bottom bar -->
        <Grid x:Name="BottomBar" Grid.Row="8" Padding="10,6,10,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Background="Transparent" BorderThickness="0"
                    Padding="6,3" Click="ChooseFolder_Click">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE838;" FontSize="12"/>
                    <TextBlock Text="Choose Folder"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               FontSize="11"/>
                </StackPanel>
            </Button>

            <TextBlock x:Name="TrackCountText" Grid.Column="2"
                       Text="0 tracks"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                       VerticalAlignment="Center"
                       FontSize="11"
                       Margin="0,0,6,0"/>

            <Button Grid.Column="3" Background="Transparent" BorderThickness="0"
                    Padding="6,3" Click="Settings_Click"
                    ToolTipService.ToolTip="Settings">
                <FontIcon Glyph="&#xE713;" FontSize="13"/>
            </Button>
        </Grid>
    </Grid>
</Window>
